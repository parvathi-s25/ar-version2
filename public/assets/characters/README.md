# Character assets

Place your own `.glb` or `.gltf` files in this folder and update `public/story/sample-story.json`.

The included `sample-character.gltf` is a tiny placeholder model used only to verify Phase 4 loading, page anchoring, movement, and timeline sync.
